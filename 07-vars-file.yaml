- name: variables from files
  hosts: local
  connection: local
  vars_files:
    - course.yaml
  tasks:
    - name: print corse information
      ansible.builtin.debug:
        msg: "course is {{ COURSE }} , duration is {{ DURATION}}, trainer is {{ TRAINER }}"
